@model IEnumerable<CRMYonetimSistemi.Models.Payment>

@{
    ViewData["Title"] = "Ödemeler";
}

<h1>Ödeme Kayıtları</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Yeni Ödeme Ekle</a>
</p>
<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>
                Müşteri
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Amount) (₺)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @* İlişkili müşterinin adını gösterir *@
                    @Html.DisplayFor(modelItem => item.Customer!.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Amount)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Date)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning">Düzenle</a> |
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-info">Detaylar</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Sil</a>
                </td>
            </tr>
        }
    </tbody>
</table>
